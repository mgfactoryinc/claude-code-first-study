# CLAUDE.md

このファイルは、**Claude Code がこのフォルダで作業するときに、いちばん最初に読む「お願いレター」**です。

漫画の第6話で「作業フォルダには CLAUDE.md を置くのがオススメ」と説明していますが、
これがその実物です。**書き方の見本としても、そのまま読んでみてください。**
むずかしい書式はありません。新しく入った人に渡す業務メモのつもりで書けば大丈夫です。

---

## このリポジトリについて

プログラミング未経験の人に「バイブコーディング」を紹介するための、**教材**です。

- `docs/index.html` — 漫画ビューア（全14見開き。紙が180度めくれる本の形式）
- `docs/manual.html` — 実践マニュアル（実際の画面のスクリーンショット付き）
- `docs/script.js` — 漫画のセリフ原稿。`window.MANGA_SCRIPT` に12話分がまとまっています
- `docs/assets/pages/` — 見開き12枚のイラスト（AI 生成の完成原稿）
- `docs/images/` — マニュアル用の実画面スクリーンショット

ビルドは不要です。HTML をブラウザで開けばそのまま動きます。

---

## 話す相手について

**このリポジトリを開く人の多くは、プログラミングの初学者です。**
そのつもりで対応してください。

- 返事は、かならず**日本語**でお願いします
- 専門用語を使うときは、一度かみくだいた説明を添えてください
  - 例：「リポジトリ（＝ソースコードの保管場所）」
- 「ターミナルで◯◯して」と言う前に、VSCode の画面から同じことができないか考えてください
- ファイルを変更する前に、**何をするか一言だけ**教えてください
- うまくいかないときは、勝手に試行錯誤を続けず、**いったん止まって質問**してください
- 指示されていないことは、やらないでください

---

## 直すときに気をつけてほしいこと

### セリフを変えたい

`docs/script.js` の `MANGA_SCRIPT` が正です。各吹き出しはこういう形をしています。

```js
{ side: 'L', pos: { top: '6%', left: '6%' }, tail: 'br', speaker: 'akari',
  text: 'あの、それって\n何ですか？' }
```

- `side` — `'L'` なら左ページ、`'R'` なら右ページ
- `pos` — そのページの中での位置（％）
- `tail` — 吹き出しのしっぽの向き。`bl` `br` `tl` `tr` `none`
- `speaker` — `akari` / `ojisan` / `narration` / `ojisan-narration`

### 吹き出しの位置を動かすとき

**`pos` の数字は、イラストを見ながら手で合わせてあります。**
「しっぽが、しゃべっている本人を指しているか」「顔を隠していないか」が基準です。
動かしたあとは、かならずブラウザで開いて目で確認してください。

### イラストについて

`docs/assets/pages/*.png` は AI 生成の完成原稿です。
**セリフに合わせて絵を描き直すことはできません。** 絵に合わせてセリフの位置を調整してください。

### 読む向き

**左綴じ（左 → 右読み）**です。セリフも左ページ → 右ページの順に流れます。
→ キーで進み、右ページが左へめくれます。ここを反転させないでください。

### 画像を追加するとき

**Git LFS は使わないでください。** GitHub Pages は LFS のファイルを配信できず、
画像が表示されなくなります。このリポジトリの画像は普通の Git で管理しています。

---

## やってほしくないこと

- `LICENSE` を書き換えること（CC BY-NC 4.0 の公式テキストを無改変で置いています）
- 読む向きを右 → 左に変えること
- 漫画とマニュアルのデザイン（色・フォント）をバラバラにすること
  - 色とフォントの定義は `docs/index.html` と `docs/manual.html` の `:root` にあります。両方そろえてください

---

## 動作確認のしかた

ブラウザで直接開いてもだいたい動きますが、きちんと確認するならローカルサーバーを立ててください。

```
cd docs
python -m http.server 8000
```

そのあとブラウザで <http://localhost:8000/> を開きます。

確認するポイント：

- 表紙 → 第12話 → 裏表紙まで、→ キーで最後まで進めるか
- 途中でページのめくりアニメーションが崩れていないか
- 吹き出しが、しゃべっている人の顔を隠していないか
- マニュアルの画像 5 枚がすべて表示されるか
